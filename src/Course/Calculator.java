package Course;

import javax.swing.*;
import java.awt.*;

public class Calculator {

    public static void main(String[] args) {

        /*
        var mainFrameTitle = "JFrame title goes here";
        JFrame frame = new JFrame(); // JFrame = a GUI window to add components to.
        frame.setTitle(mainFrameTitle); // Set JFrame title.
        frame.setSize(420, 420); //Sets the x-dimension and y-dimension of our frame.
        frame.setResizable(false); // Prevent frame from being resizable.
        frame.setVisible(true); // Make frame visible.
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // Exit out of application.

        ImageIcon image = new ImageIcon("logo.png"); // Create an ImageIcon.
        frame.setIconImage(image.getImage()); // Change icon of frame.
        frame.getContentPane().setBackground(Color.LIGHT_GRAY); // Change color of background. Can use new Color(RBG, RBH, RBG) or hexadecimals.
        */


        /*
         Llamamos a la clase MyFrame con su constructor sin parametros que se encarga de instanciar
         la interfaz con su comportamiento establecido.
         ****/
        MainFrame mainFrame = new MainFrame();


    }

}
